# Database

The schema is workspace-ready, with `workspace_id` on business entities. Unique constraints enforce recipient deduplication, reusable template keys, webhook idempotency, one recipient per campaign, and one provider broadcast per campaign/wave/variant.
