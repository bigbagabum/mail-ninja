# Deployment

Requirements: Docker support, PostgreSQL 16, persistent storage, HTTPS, public webhook URL, environment secrets, separate web and worker processes, and database backups. No hosting provider is assumed.
