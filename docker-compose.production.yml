services:
  web:
    restart: unless-stopped
    environment:
      NODE_ENV: production
  worker:
    restart: unless-stopped
    environment:
      NODE_ENV: production
  postgres:
    restart: unless-stopped
